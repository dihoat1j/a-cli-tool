[project]
name = "a_cli_tool"
version = "0.1.0"
description = "Production-ready a-cli-tool toolkit for multi-agent orchestration and shared memory."
requires-python = ">=3.10"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]
